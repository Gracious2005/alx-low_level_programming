#include "main.h"
		
/**
 * _isdigit - examine  for digit 0 - 9
 * @c: integer to  check
 * Return: 1 if integer c is a digit number, if not return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
